mysql:
  extends:
    file: ../db/mysql.yml
    service: mysql

drupal:
  container_name: drupal
  image: samos123/drupal
  environment:
    DB_HOST: mysql
    DB_PASS: ${db_pass}
    ADMIN_PASSWORD: ${admin_pass}
  ports:
    - "80:80"
